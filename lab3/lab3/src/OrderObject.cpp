#include <vector>
using namespace std;

namespace Tribbler{

class OrderObject {
public:
    vector<int> vt;
    int hostid;

    void compare(OrderObject& vc2)
    {
        bool v1Bigger = false;
        bool v2Bigger = false;
        int p1 = 0;
        int p2 = 0;
        while(p1 < vt.size() && p2 < vc2.vt.size())
        {
            
        }
    }
};



};
